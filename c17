#include<stdio.h>
int main()
{
	int a;
	printf("Enter the age :");
	scanf("%d",&a);
	if(a>=18)
	{
		printf("eligible for vote:");
	}
	else if(a<18)
	{
		printf(" alert Not eligble for vote");

			printf("\nRemainding age for vote:%d",18-a);
		}
	
	return 0;
}
